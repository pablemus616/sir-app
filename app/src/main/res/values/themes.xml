<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base de la app tras el splash (la UI real es Compose / SirTheme). -->
    <style name="Theme.SirMobile" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash de Android 12+ (androidx.core:core-splashscreen): fondo blanco + logo SIR. -->
    <style name="Theme.SirMobile.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.SirMobile</item>
    </style>
</resources>
